services:
  api:
    build: apis
    ports:
      - "3000:3000"
    depends_on:
      - db
    env_file:
      - apis/.env

  db:
    build: db
    ports:
      - "6379:6379"
    volumes:
      - .data:/data
